import { HolaDirective } from './hola.directive';

describe('HolaDirective', () => {
  it('should create an instance', () => {
    const directive = new HolaDirective();
    expect(directive).toBeTruthy();
  });
});
